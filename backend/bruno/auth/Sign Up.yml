info:
  name: Sign Up
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/auth/sign-up
  body:
    type: json
    data: |-
      {
        "email": "roman.sarder.js@gmail.com",
        "password": "1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
