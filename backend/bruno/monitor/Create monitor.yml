info:
  name: Create monitor
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/monitors
  body:
    type: json
    data: |-
      {
        "name": "Painpoints",
        "sources": [
          "hs"
        ],
        "keywords": [
          "Ask HN: how do I",
          "struggling with React",
          "anyone solved",
          "frontend performance",
          "TypeScript help"
        ],
        "intervalMinutes": 10080
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
