info:
  name: Sign In
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/auth/sign-in
  body:
    type: json
    data: |-
      {
        "email": "roman.sarder.js@gmail.com",
        "password": "1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
